# 🧮 Многопоточное суммирование массива на C++

## 📘 Описание
Данная программа демонстрирует разницу в скорости между **однопоточным** и **многопоточным** вычислением суммы элементов массива.  
Программа создаёт массив из 1 000 000 случайных чисел и вычисляет их сумму:
- сначала **в одном потоке** (последовательно),
- затем **в четырёх потоках** (параллельно).

Результаты показывают, насколько многопоточность ускоряет выполнение программы.

---

## ⚙️ Используемые технологии
- **C++17**
- **std::thread** — для создания потоков  
- **std::chrono** — для измерения времени  
- **std::vector** — для хранения данных  
